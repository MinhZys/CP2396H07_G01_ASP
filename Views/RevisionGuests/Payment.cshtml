@model Symphony.Portal.Web.Models.RevisionRegistration

@{
    ViewData["Title"] = "Thanh toán lớp ôn thi";
    var fee = Model.RevisionPackage?.Fee ?? 0;
}

<div class="min-h-screen bg-background-dark py-12 px-4">
    <div class="max-w-4xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-8">

        <!-- THÔNG TIN -->
        <div class="bg-surface-dark border border-slate-800 rounded-2xl p-8">
            <h3 class="text-xl font-bold text-white mb-6">Thông tin đăng ký</h3>

            <div class="space-y-4">
                <div class="flex justify-between">
                    <span class="text-slate-400">Họ tên</span>
                    <span class="text-white">@Model.FullName</span>
                </div>
                <div class="flex justify-between">
                    <span class="text-slate-400">Email</span>
                    <span class="text-white">@Model.Email</span>
                </div>
                <div class="flex justify-between">
                    <span class="text-slate-400">Gói ôn</span>
                    <span class="text-primary font-bold">@Model.RevisionPackage?.Title</span>
                </div>
            </div>

            <div class="border-t border-slate-700 mt-6 pt-4 flex justify-between">
                <span class="text-slate-400">Tổng tiền</span>
                <span class="text-3xl font-bold text-white">
                    @fee.ToString("N0")đ
                </span>
            </div>
        </div>

        <!-- THANH TOÁN -->
        <div class="bg-surface-dark border border-slate-800 rounded-2xl p-8">
            <h3 class="text-xl font-bold text-white mb-6">Phương thức thanh toán</h3>

            <form asp-action="ConfirmPayment" method="post">
                @Html.AntiForgeryToken()

                <input type="hidden" name="id" value="@Model.Id" />

                <div class="space-y-4 mb-6">
                    <label class="block">
                        <input type="radio" name="paymentMethod" value="Online" checked />
                        <span class="ml-2 text-white">Thanh toán Online (VNPay)</span>
                    </label>

                    <label class="block">
                        <input type="radio" name="paymentMethod" value="Cash" />
                        <span class="ml-2 text-white">Thanh toán tiền mặt</span>
                    </label>
                </div>

                <button type="submit"
                        class="w-full bg-primary text-background-dark font-bold py-3 rounded-lg"
                        onclick="this.disabled=true; this.form.submit();">
                    Xác nhận thanh toán
                </button>
            </form>

        </div>
    </div>
</div>
