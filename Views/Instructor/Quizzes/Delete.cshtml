@model Symphony.Portal.Web.Models.Quiz

@{
    ViewData["Title"] = "Delete Quiz";

}

<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
    <h1 class="h2 text-danger">Delete Quiz</h1>
    <a asp-action="Index" class="btn btn-outline-secondary btn-sm">
        <i class="fas fa-arrow-left"></i> Back to List
    </a>
</div>

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow-sm border-danger">
            <div class="card-header bg-danger text-white">
                <i class="fas fa-exclamation-triangle"></i> Warning
            </div>
            <div class="card-body text-center p-4">
                <h4 class="card-title fw-bold">Are you sure?</h4>
                <p class="card-text text-muted">
                    This action cannot be undone. You are about to delete the quiz 
                    <strong>@Model.Name</strong>.
                </p>
                <p class="text-muted">Target Lesson: <strong>@Model.Lesson?.Title</strong></p>

                <form asp-action="Delete">
                    <input type="hidden" asp-for="Id" />
                    <div class="d-grid gap-2 mt-4">
                        <button type="submit" class="btn btn-danger btn-lg">
                            <i class="fas fa-trash-alt me-2"></i> Confirm Delete
                        </button>
                        <a asp-action="Index" class="btn btn-light">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
