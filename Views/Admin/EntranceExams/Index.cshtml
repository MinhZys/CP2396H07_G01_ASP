@model IEnumerable<Symphony.Portal.Web.Models.EntranceExam>

@{
    ViewData["Title"] = "Quản lý Kỳ thi Tuyển sinh";
}

<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="fw-bold text-slate-800 mb-1">Kỳ thi Tuyển sinh</h2>
            <p class="text-slate-500 mb-0">Quản lý danh sách các kỳ thi tuyển sinh đầu vào.</p>
        </div>
        <a asp-action="Create" class="btn btn-primary d-flex align-items-center gap-2 px-4 rounded-pill shadow-sm">
            <span class="material-symbols-outlined">add_circle</span>
            Tạo kỳ thi mới
        </a>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success border-0 shadow-sm rounded-4 d-flex align-items-center gap-3 mb-4" role="alert">
            <span class="material-symbols-outlined">check_circle</span>
            <div>@TempData["Success"]</div>
            <button type="button" class="btn-close ms-auto" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="card border-0 shadow-sm rounded-4 overflow-hidden">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="bg-light">
                    <tr>
                        <th class="px-4 py-3 text-secondary text-uppercase small fw-bold">Tên kỳ thi</th>
                        <th class="px-4 py-3 text-secondary text-uppercase small fw-bold">Ngày tổ chức</th>
                        <th class="px-4 py-3 text-secondary text-uppercase small fw-bold">Môn thi</th>
                        <th class="px-4 py-3 text-secondary text-uppercase small fw-bold">Trạng thái</th>
                        <th class="px-4 py-3 text-secondary text-uppercase small fw-bold text-end">Hành động</th>
                    </tr>
                </thead>
                <tbody class="border-top-0">
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="px-4 py-3">
                                <span class="fw-bold text-slate-700">@item.Title</span>
                            </td>
                            <td class="px-4 py-3 text-slate-600">
                                @item.ExamDate.ToString("dd/MM/yyyy HH:mm")
                            </td>
                            <td class="px-4 py-3">
                                <span class="badge bg-light text-slate-600 border px-3 rounded-pill">@item.Subjects</span>
                            </td>
                            <td class="px-4 py-3">
                                @if (item.IsActive)
                                {
                                    <span class="badge bg-success-subtle text-success border border-success-subtle px-3 rounded-pill">Đang hoạt động</span>
                                }
                                else
                                {
                                    <span class="badge bg-secondary-subtle text-secondary border border-secondary-subtle px-3 rounded-pill">Ngừng hoạt động</span>
                                }
                                
                                @if (item.IsRegistrationOpen)
                                {
                                    <span class="badge bg-info-subtle text-info border border-info-subtle px-3 rounded-pill ms-1">Mở đăng ký</span>
                                }
                            </td>
                            <td class="px-4 py-3 text-end">
                                <div class="d-flex justify-content-end gap-2">
                                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary rounded-pill px-3">
                                        <i class="fas fa-edit me-1"></i> Sửa
                                    </a>
                                    <form asp-action="Delete" asp-route-id="@item.Id" method="post" onsubmit="return confirm('Bạn có chắc chắn muốn xóa kỳ thi này?')">
                                        <button type="submit" class="btn btn-sm btn-link text-danger text-decoration-none rounded-pill px-3 hover-bg-danger-subtle">
                                            <i class="fas fa-trash-alt me-1"></i> Xóa
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        @if (!Model.Any())
        {
            <div class="text-center py-5">
                <span class="material-symbols-outlined display-4 text-slate-200 mb-3">event_busy</span>
                <p class="text-slate-500 mb-0">Chưa có kỳ thi nào được tạo.</p>
            </div>
        }
    </div>
</div>
